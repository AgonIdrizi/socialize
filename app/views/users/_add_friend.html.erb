  <%= form_for( current_user.friend_requests_sent_relationship.build, remote: true) do |f| %>
    <%= f.hidden_field :accepter_id, value: user.id %>
    <%= f.submit "Add Friend", class: 'btn btn-primary btn-sm add-button' %>
  <% end %>
