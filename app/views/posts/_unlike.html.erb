<% like = post.likes.where(user_id: current_user.id).pluck(:id) %>
<%= link_to icon('fas', 'thumbs-up')+ 'Unlike', 
                post_like_path(post,like ),:params => { :post_id => post.id },method: :delete, remote: true%></br>