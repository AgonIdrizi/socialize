<%= form_for post do |f| %>
  <%= label_tag 'New Post' %></br>
  <%= f.text_area :body,placeholder: 'Add a new post', class: "form-control" %></br>
  <%= f.submit 'New Post',class:'btn btn-primary' %>
<% end %>